# Minimal Supabase CLI config for East Coast Labs.
# Migrations are applied against the remote project via `supabase db push
# --db-url "$SUPABASE_DB_URL"`, so only project_id is needed here.
project_id = "eastcoastlabs"
